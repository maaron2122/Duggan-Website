<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Midnight Drop — Cardano Privacy</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <style>
      body {
        background-color: #0b0f14;
        color: #e5e7eb;
        padding-top: 72px;
      }

      .backdrop-blur {
        backdrop-filter: blur(8px);
        -webkit-backdrop-filter: blur(8px);
      }

      .navbar.scrolled {
        box-shadow: 0 6px 24px rgba(0, 0, 0, 0.5);
      }

      .bg-midnight {
        position: relative;
        background: url("lock.jpeg") center / cover no-repeat;
        min-height: 70vh;
        display: flex;
        align-items: center;
        background-size: 110%;
        animation: fadeIn 1.3s ease both;
      }

      @media (min-width: 992px) {
        .bg-midnight {
          background-attachment: fixed;
        }
      }

      @media (max-width: 768px) {
        .bg-midnight {
          min-height: 60vh;
          background-position: center top;
          background-size: 140%;
        }
      }

      .bg-midnight::before {
        content: "";
        position: absolute;
        inset: 0;
        background: rgba(2, 6, 23, 0.72);
      }

      .bg-midnight > .container {
        position: relative;
        z-index: 1;
      }

      .hero-glow {
        text-shadow: 0 2px 18px rgba(0, 0, 0, 0.75);
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(6px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .text-muted {
        color: #9ca3af !important;
      }

      .card-dark {
        background-color: #111827;
        border: 1px solid #1f2937;
      }

      .form-dark {
        background-color: #020617;
        border: 1px solid #1f2937;
      }

      .form-control {
        background-color: #020617;
        color: #e5e7eb;
        border: 1px solid #1f2937;
      }

      .form-control:focus {
        background-color: #020617;
        color: #fff;
        border-color: #6366f1;
        box-shadow: none;
      }

      footer {
        color: #6b7280;
      }

      .text-accent-white {
        color: #ffffff !important;
      }
    </style>
  </head>

  <body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-black bg-opacity-75 border-bottom border-secondary border-opacity-25 fixed-top backdrop-blur">
      <div class="container">
        <a class="navbar-brand fw-bold" href="#">Midnight Drop</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navMenu">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item"><a class="nav-link" href="#what">What</a></li>
            <li class="nav-item"><a class="nav-link" href="#why">Why</a></li>
            <li class="nav-item"><a class="nav-link" href="#contact">Contact</a></li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Hero -->
    <header class="bg-midnight text-center">
      <div class="container py-5 hero-glow">
        <h1 class="display-5 fw-bold">Midnight Drop</h1>
        <p class="lead text-muted mb-4">Privacy infrastructure for the Cardano ecosystem.</p>
        <a href="#contact" class="btn btn-primary btn-lg">Contact Me</a>
      </div>
    </header>

    <!-- What -->
    <section id="what" class="py-5">
      <div class="container">
        <h2 class="fw-bold mb-3">What is Midnight Drop?</h2>
        <p class="text-muted">
          Midnight Drop is a community-driven educational and awareness initiative focused on privacy,
          compliance, and user-controlled data on Cardano.
        </p>
        <p class="text-muted">
          It exists to help users understand how decentralized systems can offer privacy without sacrificing trust or legality.
        </p>
      </div>
    </section>

    <!-- Why -->
    <section id="why" class="py-5 bg-black bg-opacity-50">
      <div class="container">
        <h2 class="fw-bold mb-4">Why Midnight?</h2>
        <div class="row g-4">
          <div class="col-md-4">
            <div class="card card-dark h-100">
              <div class="card-body">
                <h3 class="h5 fw-semibold text-accent-white">Privacy by Design</h3>
                <p class="text-muted mb-0">Built for selective disclosure and confidentiality.</p>
              </div>
            </div>
          </div>

          <div class="col-md-4">
            <div class="card card-dark h-100">
              <div class="card-body">
                <h3 class="h5 fw-semibold text-accent-white">Cardano Native</h3>
                <p class="text-muted mb-0">Integrates with Cardano’s secure and decentralized infrastructure.</p>
              </div>
            </div>
          </div>

          <div class="col-md-4">
            <div class="card card-dark h-100">
              <div class="card-body">
                <h3 class="h5 fw-semibold text-accent-white">User-Controlled Data</h3>
                <p class="text-muted mb-0">You decide what information is shared — not platforms.</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Contact -->
    <section id="contact" class="py-5">
      <div class="container">
        <div class="row g-4">
          <div class="col-lg-6">
            <h2 class="fw-bold mb-2">Contact</h2>
            <p class="text-muted">Questions, collaboration, or feedback? Reach out.</p>
          </div>
          <div class="col-lg-6">
            <form class="form-dark p-4 rounded-4">
              <div class="mb-3">
                <label class="form-label">Name</label>
                <input class="form-control" placeholder="Your name" />
              </div>
              <div class="mb-3">
                <label class="form-label">Email</label>
                <input class="form-control" type="email" placeholder="you@example.com" />
              </div>
              <div class="mb-3">
                <label class="form-label">Message</label>
                <textarea class="form-control" rows="4" placeholder="Your message..."></textarea>
              </div>
              <button type="button" class="btn btn-primary w-100">Send</button>
              <div class="form-text text-muted mt-2">This form is informational only.</div>
            </form>
          </div>
        </div>
      </div>
    </section>

    <!-- Footer -->
    <footer class="py-4 border-top border-secondary border-opacity-25 text-center">
      © <span id="year"></span> Midnight Drop — Not financial advice.
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
      document.getElementById("year").textContent = new Date().getFullYear();

      const nav = document.querySelector(".navbar");
      window.addEventListener("scroll", () => {
        nav.classList.toggle("scrolled", window.scrollY > 10);
      });
    </script>
  </body>
</html>
